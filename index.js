import 'dotenv/config'; import express from 'express'; import path from 'path'; import { fileURLToPath } from 'url'; const __dirname = path.dirname(fileURLToPath(import.meta.url)); const app = express(); const PORT = process.env.PORT || 3001; app.use(express.json()); app.get('/api/health', (req, res) => { res.json({ status: 'server is running' }); }); app.use(express.static(__dirname)); app.use((req, res, next) => { if (req.path.split('/')[1] !== 'api') { console.log('Boutique identifiee:', req.path.split('/')[1]); return res.sendFile(path.join(__dirname, 'index.html')); } next(); }); app.listen(PORT, () => { console.log('Server listening on port ' + PORT); });
